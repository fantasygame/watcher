.container
  .row
    %h3 Currently watching
  - @tvs.in_groups_of(4, false).each do |group|
    .row
      - group.each do |tv|
        .col-lg-3
          %table
            %tr
              %td
                = link_to tv_path(tv.id), method: :get do
                  != logo_image_path(tv.poster_path, 3, tv.name)
            %tr
              %td= "#{tv.name} #{tv.year}"
