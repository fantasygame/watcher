!!!
%html
  %head
    %meta{content: "width=device-width, initial-scale=1.0", name: "viewport"}
      %title= content_for?(:title) ? yield(:title) : "The Watcher"
      %meta{content: content_for?(:description) ? yield(:description) : "Get your shows together", name: "description"}
        = stylesheet_link_tag 'application', media: 'all'
        = javascript_include_tag 'application'
        = csrf_meta_tags
        = yield :js
  %body
    %header
      = render 'layouts/navigation'
    = render 'layouts/messages'
    .container
      = yield
