%tr
  %td= episode.season_marker
  %td= episode.name
  %td
    - if current_user.seen?(episode)
      = link_to 'Set as unseen', view_path(id: episode.id), method: :delete
    - else
      = link_to 'Set as seen', views_path(view: {tv_id: tv.id, episode_id: episode.id}), method: :post
